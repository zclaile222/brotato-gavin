# Brotato-Gavin 开发报告 v0.1.0

> 生成时间：2026-03-26 | 引擎：Godot 4.x | 功能总计：40项

---

## 一、游戏概述

**类型：** 2D 俯视角 Roguelite 生存射击（Brotato 同类型克隆）
**引擎：** Godot 4.x（GDScript，GL Compatibility 渲染）
**分辨率：** 1280×720
**开发方式：** 纯代码（无场景编辑器 UI，所有界面在 `_ready()` 中程序化生成）

---

## 二、核心玩法

玩家选择一名角色后进入战场，游戏以 **波次（Wave）** 为单位循环推进：

```
战斗阶段（20秒）→ 商店阶段（购买装备/升级）→ 下一波
```

- **战斗目标：** 在限定时间内存活，击杀场上所有或尽可能多的敌人
- **成长路径：** 击杀获得 XP 升级，击杀/商店获得金币，商店购买武器和被动道具
- **游戏结束条件：** HP 归零（失败）或坚持 20 波（胜利）
- **无尽模式：** 通关后可继续挑战，波次无上限，难度持续提升

---

## 三、系统模块详解

### 3.1 角色系统（`GameState.gd` + `CharacterSelect.gd`）

目前共 **14 名可选角色**：

| 角色 | 特色 | 解锁条件 |
|------|------|----------|
| 普通人 | 均衡属性 | 默认 |
| 战士 | HP+5 护甲+1，速度慢 | 默认 |
| 猎手 | 射速+30% 暴击+8% | 默认 |
| 枪手 | 双持手枪 | 通关一次 |
| 狂战士 | HP越低伤害越高（被动） | 单波30击杀 |
| 工程师 | 每15秒自动部署炮台 | 购买10次升级 |
| 忍者 | 速度+200 暴击+20% | 困难通关 |
| 巫师 | 伤害+3 等离子炮起手 | 达到20级 |
| 骑士 | HP+8 护甲+5，极慢 | 受伤500次存活 |
| 弓箭手 | 弩起手 精准特性 | 15波前100击杀 |
| 商人 | 幸运+20 初始金30 | 单局积200金 |
| 暴徒 | 伤害+2 暴击伤害2.5x | 连击10x |
| 幽灵 | 25%完全闪避 | 闪避50次 |
| 死灵法师 | 击杀10%概率召唤亡灵 | 累计500击杀 |

**角色差异化被动（4种已实现）：**

- **工程师：** `request_turret` 信号 → Main.gd 生成绿色炮台节点，持续20秒，自动射击
- **狂战士：** `_physics_process` 中实时计算 HP 阈值加成（75%/50%/25% 各+3伤害）
- **幽灵：** `take_damage()` 中25%概率 damage=0，触发半透明闪烁动画
- **死灵法师：** `request_undead` 信号 → Main.gd 生成紫色亡灵节点，跟随玩家攻击敌人

---

### 3.2 武器系统（`Player.gd`）

共 **16 种武器**，分近战和远程两大类：

**远程武器：** 手枪、散弹枪（5发扇形）、狙击枪（4×伤害）、机关枪、弩、激光枪（穿透）、火箭筒（溅射）、等离子炮（蓄力）、回旋镖（自动返回）、飞刀（三连发）、手雷（重力弹道+溅射）、火焰喷射器（短距）、速射枪、冲锋枪

**近战武器：** 剑（范围攻击）、长矛（穿透投掷）

**子弹弹道特性（`Bullet.gd`）：**

| 属性 | 效果 |
|------|------|
| `pierce` | 穿透（最多3个目标） |
| `splash` | 溅射（范围内50%伤害） |
| `returns` | 回旋返回玩家 |
| `gravity` | 重力弹道下坠 |
| `chain_lightning` | 命中后弹射至200px内最近敌人（50%伤害） |
| `ammo_type` | 元素属性（火焰燃烧/冰霜减速/闪电眩晕） |

**武器升级（`Shop.gd`）：** 每件武器可在商店强化至5级，每级伤害×1.25、射速×1.1，3级解锁穿透特效，5级变金色加★

---

### 3.3 敌人系统（`Enemy.gd`）

共 **15 种敌人类型**，各有独特行为：

| 类型 | 特色 |
|------|------|
| normal | 基础近战冲锋 |
| fast | 高速低血 |
| tank | 高血高护甲，慢速 |
| ranged | 远程射击，保持距离 |
| shooter_spread | 扇形多弹射击 |
| exploder | 接近玩家爆炸 |
| healer | 定期给附近敌人治疗 |
| swarm | 极速虫群，低血高接触伤害 |
| armored | 子弹伤害减免 |
| ghost | 周期性无敌切换 |
| charger | 蓄力冲刺攻击 |
| summoner | 定期召唤小怪 |
| elite | 随机获得额外特性 |
| miniboss | 精英大怪 |
| boss | Boss（含3阶段强化） |

**AI 机制：**

- Boids 分离力（`_get_separation_force()`）防止敌人叠加，每2帧缓存一次
- 远程敌人维持偏好距离，近战敌人直接冲锋
- **Boss 三阶段：** HP 75%/40%/15% 触发，速度/攻击模式逐阶强化，螺旋弹幕/狂暴爆发
- **状态效果：** 燃烧 DOT、减速（冰霜弹药）、眩晕（闪电弹药）、冻结

---

### 3.4 商店系统（`Shop.gd`）

**4级稀有度体系：**

| 等级 | 颜色 | 价格倍率 | 特效 |
|------|------|---------|------|
| 普通 | 白色 | ×1.0 | 无 |
| 非凡 | 绿色 | ×1.5 | 无 |
| 稀有 | 蓝色 | ×2.2 | 无 |
| 传说 | 金色 | ×3.5 | 金色脉冲动画 |

共约 **75 种道具**（武器16 + 被动55 + 元素弹药3），按幸运值（luck）权重随机展示5张。

**商店功能：**
- 刷新（每次消耗金币）
- 武器升级区（1~5级，成本递增）
- 出售道具（按购入价50%回收）

---

### 3.5 波次与难度系统（`Main.gd`）

**波次结构：**

- 第1~20波：每波20秒，第5/10/15/20波出Boss
- Boss波：`spawn_boss()` + `_spawn_miniboss()` 同时生成
- 无尽模式（20波后）：`_get_endless_multiplier()` 每波+8%强度倍率，30波额外miniboss，40波额外boss

**10种波次修饰器（随机触发）：**

急速 / 虫群 / 精英波 / 金币雨 / 双倍XP / 装甲波 / Boss狂潮 / 生命涌动 / 迷你 / 无修饰

**5种特殊波次事件（波次开始前3秒倒计时选择）：**

| 事件 | 效果 |
|------|------|
| 治愈之泉 | 消耗15金，回满HP |
| 宝箱 | 击杀10个敌人获得30金 |
| 伏击 | 敌人数量翻倍，击杀额外+3金 |
| 祝福 | 本波伤害+30%、速度+20% |
| 诅咒 | 敌人HP+50%，但XP×2 |

**动态难度系统（每5秒采样）：**

- HP>80% 且击杀>10/5秒 → `difficulty_scale` +0.05（上限2.0，变难）
- HP<30% 或5秒受伤>3次 → `difficulty_scale` -0.05（下限0.5，变易）
- 影响：敌人生成间隔 = `spawn_interval / difficulty_scale`

---

### 3.6 道具协同系统（`Player.gd` SYNERGIES）

7种装备组合触发被动协同加成：

| 协同名称 | 触发条件 | 加成效果 |
|---------|---------|---------|
| 火焰大师 | 火焰喷射器 + 燃烧附魔 | 燃烧伤害+100%，燃烧概率+20% |
| 冰河时代 | 冰冻弹片 + 多件武器 | 减速效果+50% |
| 死亡之舞 | 吸血 + 伤害加成 | 击杀回1HP |
| 钢铁堡垒 | 护甲≥4 + HP≥15 | 护甲+3，每波回血+2 |
| 吸血鬼 | 吸血≥10% + 复仇 | 吸血+10% |
| 速度恶魔 | 肾上腺素 + 速度≥400 | 速度+60，暴击+10% |
| （可扩展） | 更多装备组合 | — |

---

### 3.7 HUD / UI 系统（`HUD.gd`）

全部程序化构建（约1200+行），各区域布局：

| 位置 | 内容 |
|------|------|
| 左上 | HP条、XP条、等级、金币、武器列表 |
| 中上 | 波次计数器、倒计时、Boss血条 |
| 左下 | 临时Buff倒计时图标（速度/伤害/急速射/护盾/吸血） |
| 右上 | 雷达式小地图（玩家居中，敌人红点，拾取物彩色点） |
| 右上（小地图下） | 成就解锁金色滑入通知 |
| 右上角 | 角色被动状态指示器 |
| 屏幕中央 | 连击计数（2x~20x+），里程碑金色闪烁 |
| 屏幕边缘 | 敌人方向箭头（超出视野时提示） |
| 全屏 | 低血量红色暗角晕影（HP≤30%显示，HP≤15%脉冲） |
| ESC | 暂停菜单（继续/音量/重开/返回主菜单，含确认弹窗） |

---

### 3.8 其他系统

| 系统 | 实现位置 | 说明 |
|------|---------|------|
| 对象池 | Main.gd | 子弹×80、敌人子弹×60、XP球×40，动态扩容 |
| 战场战利品 | Main.gd + Player.gd | 敌人15%/精英30%掉落临时增益，5种类型，自动拾取80px |
| 元素弹药 | Bullet.gd + Enemy.gd | 火焰（燃烧DOT）/冰霜（减速30%）/闪电（20%眩晕） |
| 成就系统 | SaveSystem.gd | 10个成就，持久化JSON存档，游戏结束展示新解锁 |
| 存档系统 | SaveSystem.gd | 记录最高波次/击杀/无尽记录/成就/已解锁角色 |
| 程序化音效 | AudioManager.gd | 战斗BGM、心跳音效（HP≤30%）、AudioStreamGenerator生成 |
| 特效 | Effects.gd | 全局单例，命中火花、升级闪光 |
| 摄像机抖动 | Main.gd | 受伤时10px抖动，HP≤20%时持续±1px微抖动 |

---

## 四、项目文件结构

```
brotato-gavin/
├── scenes/
│   ├── Main.tscn            # 主游戏场景（含 Player 节点）
│   ├── Player.tscn          # 玩家角色
│   ├── Enemy.tscn           # 敌人（复用所有类型）
│   ├── Bullet.tscn          # 子弹（对象池）
│   ├── EnemyBullet.tscn     # 敌人子弹（对象池）
│   ├── XPOrb.tscn           # XP球（对象池）
│   ├── HUD.tscn             # UI层
│   ├── Shop.tscn            # 商店
│   ├── CharacterSelect.tscn # 角色选择
│   └── MainMenu.tscn        # 主菜单
├── scripts/
│   ├── Main.gd              # 游戏主控制器
│   ├── Player.gd            # 玩家逻辑（武器/被动/协同）
│   ├── Enemy.gd             # 敌人AI与类型系统
│   ├── Bullet.gd            # 子弹行为
│   ├── HUD.gd               # 全部UI（1200+行）
│   ├── Shop.gd              # 商店与道具池
│   ├── GameState.gd         # 角色定义（Autoload）
│   ├── SaveSystem.gd        # 存档与成就（Autoload）
│   ├── Effects.gd           # 特效工具（Autoload）
│   ├── AudioManager.gd      # 音效系统
│   ├── XPOrb.gd             # XP球行为
│   ├── CharacterSelect.gd   # 角色选择界面
│   └── MainMenu.gd          # 主菜单界面
├── DEV_REPORT.md            # 本开发报告
└── CHANGELOG.md             # 功能版本记录
```

**Autoloads（全局单例）：** `GameState` · `Effects` · `SaveSystem`

> ⚠️ **注意：** `AudioManager` 未注册为 Autoload，需在 `project.godot` 的 `[autoload]` 段手动添加才能全局访问（目前用 `has_node("/root/AudioManager")` 安全检查代替）。

---

## 五、可扩展与修改内容（策划方向）

### 5.1 内容扩充（低难度，直接加数据）

**新武器**（在 `Player.gd` WEAPON_DATA 添加字典）：
- 狙击炮：高伤害 + 穿透 + 极慢射速
- 魔法棒：AOE 圆形攻击范围
- 投石机：重力弹道 + 超大溅射半径
- 链锯：近战持续接触伤害，无射程限制

**新敌人**（在 `Enemy.gd` ENEMY_TYPES 添加字典）：
- 护盾型：受击方向免疫，需绕背攻击
- 分裂型：死亡时分裂为2个较小个体
- 镜像型：复制玩家运动轨迹
- 自爆Boss：接近时加速，死亡触发大范围爆炸

**新道具**（在 `Shop.gd` ITEM_POOL 追加条目）：
- 弹药补给：临时+2伤害，持续1波
- 时间之沙：当波倒计时+5秒
- 经验宝珠：立即升1级
- 翻倍硬币：下一波金币收入×2

**新角色**（在 `GameState.gd` CHARACTERS 追加）：
- 赏金猎人：击杀金币+100%，HP-3
- 机甲战士：护盾×2但无法通过道具回血
- 吟游诗人：范围内敌人持续减速20%

---

### 5.2 系统深化（中等难度）

**地图/障碍物系统：**
- 在 Main.gd `_ready()` 中随机放置 `StaticBody2D` 障碍节点（箱子、墙壁）
- 敌人 AI 需加入简单绕路逻辑（或直接冲撞障碍）
- 特殊地形区域：踩上去获得临时速度/伤害加成

**扩展协同效果：**
- `Player.gd` SYNERGIES 字典直接追加，目前仅7种
- 建议扩展至20+种，覆盖更多武器组合（如双持近战、全元素套装）

**多段波次修饰叠加：**
- 当前每波只有1种修饰词
- 改为同时叠加2~3种（需调整 HUD 修饰词显示布局）

**精英词缀系统完善：**
- `Enemy.gd` 的 `elite_trait` 目前仅占位，可填充实际逻辑：
  - 反弹：被子弹命中时随机弹回一颗
  - 快速再生：每秒回复1HP
  - 磁性护盾：吸引附近子弹偏转轨迹

**链式闪电多跳扩展：**
- 当前只跳1次，可改为 2~5 跳，每跳减少20%伤害
- 在 `Bullet.gd` 的链闪电逻辑中用递归或循环实现

---

### 5.3 游戏体验打磨（低~中难度）

**真实音频资源：**
- 替换 `AudioManager.gd` 的程序化生成为 `.ogg` 音效文件
- 多轨战斗 BGM（随波次强度切换不同音轨）
- 需要先将 AudioManager 注册为 Autoload

**视觉升级：**
- 用 `Sprite2D` + 贴图替换几何图形（玩家/敌人/子弹差异化外观）
- 添加粒子特效（死亡爆炸 `CPUParticles2D`、升级光圈）
- TileMap 铺设地板/装饰背景

**数值平衡调整热区：**

| 调整目标 | 位置 |
|---------|------|
| 敌人强度 | `Enemy.gd` ENEMY_TYPES 中的 `hp_m`、`spd` |
| 波次节奏 | `Main.gd` WAVE_DURATION、spawn_interval |
| 动态难度灵敏度 | `_update_difficulty()` 中的采样阈值 |
| 道具价格/稀有度 | `Shop.gd` ITEM_POOL 各条目 |
| 无尽模式缩放 | `_get_endless_multiplier()` 的系数（当前+8%/波） |

---

### 5.4 模式扩展（高难度开发）

**本地多人合作：**
- 重构 `player = get_first_node_in_group("player")` → 多玩家列表
- Main.gd 的伤害/金币/治疗系统需区分归属玩家
- HUD 需支持多个玩家状态面板

**跑分排行榜：**
- `SaveSystem.gd` 已有最高波次/击杀记录
- 扩展为本地多条历史记录，或接入网络排行榜 API

**每日挑战模式：**
- 固定随机种子 + 固定角色 + 固定事件序列
- 所有玩家同一天面对完全相同的局面，比拼得分

**元进度 / 技能树：**
- 通关后解锁永久小加成（如全角色HP+1、商店初始多一次刷新）
- 在 `SaveSystem.gd` 添加 `meta_progress` 字典，游戏启动时读取并应用

---

## 六、已知技术债务

| 问题 | 影响程度 | 建议处理 |
|------|---------|---------|
| ~~碰撞使用手动 `distance_to` 而非碰撞层~~ | ~~已修复~~ | 已迁移至 Area2D 碰撞信号 |
| AudioManager 未注册为 Autoload | 低（音量滑条需要它） | 在 `project.godot [autoload]` 添加一行 |
| HUD.gd 单文件 1200+ 行 | 中（维护困难） | 拆分为 HUD_Combat / HUD_Shop / HUD_Menu |
| 对象池初始大小固定（子弹80） | 低（已有动态扩容兜底） | 按实际武器配置提高初始值 |
| 小地图每帧重建所有 Polygon2D 子节点 | 低（0.1秒更新一次） | 改用 `_draw()` 自定义绘制 |
| `get_viewport_rect()` 在 CanvasLayer 不可用 | 已修复（改为 `get_viewport().get_visible_rect()`） | — |

---

*Brotato-Gavin v0.1.0 | 开发工具：Claude Code Agent Team（systems-dev / weapons-dev / content-dev）*
